TODO - irecorder
 *error handling
   -overwrite check.
   -error retry.
   -get real broadcast length to check ok.
   - http 404 and no rawfile.
 *installer
 *right click open file/url
 *solve confliction of generated file names.
 *error dialog for misc errors. download/convert error. and retry.
 *clear downloaded files in task win.
 *feed cache
 *auto download subscription tags

 *icon image
 *mencoder for dump
 *download from proxy (mplayer)
 *status bar for processing.
 *stylesheet -menu colour
 *log message level.
 *log limit line number
 *resume task
 *fileName + sub category
 *fileName + date
 *multilanguage?

success but message is error.
INFO :realrtsp: Stream EOF detected
INFO :Core dumped ;)
